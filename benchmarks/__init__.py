"""PrimeKV benchmark scripts.

Each script in this directory is runnable as ``python benchmarks/<name>.py``.
They share a handful of helpers (model loading, corpus, output dir) kept
local to this package to avoid coupling ``primekv/`` to argparse.
"""
